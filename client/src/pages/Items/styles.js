const styles = theme => ({
    root: {
        flexGrow: 1,
    },
    paper: {
        height: '20em',
        width: '15em',
    },
    control: {
        padding: theme.spacing.unit * 2,
    },
});

export default styles;
