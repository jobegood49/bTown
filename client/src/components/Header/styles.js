const styles = theme => ({
    header: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '6rem',
        color: 'white',
        background: '#444',

    }
});

export default styles;